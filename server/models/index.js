const Comment = require("./Comment");
const Event = require("./Event");
const { User } = require("./User"); // Desestructura el objeto y obtén el modelo User

module.exports = { Comment, Event, User };
